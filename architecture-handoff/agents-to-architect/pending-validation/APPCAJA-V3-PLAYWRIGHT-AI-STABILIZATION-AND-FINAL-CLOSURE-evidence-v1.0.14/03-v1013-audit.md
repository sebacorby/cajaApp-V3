# v1.0.13 Audit

## What Was Accepted from v1.0.13

The following changes from v1.0.13 were accepted and frozen:

1. `normalizeAdvisorPeriod()` function - extracts only `{from, to}` from any input
2. Normalization of period in `context()` and `ask()` methods
3. `buildContextFingerprint()` helper for centralized fingerprint computation
4. Fingerprint independent of `mode`, `currency`, and `question`
5. Deterministic simulation fingerprints
6. 6 new fingerprint consistency tests (154 total backend tests)
7. Backend: 154/154 tests PASS
8. Smoke: 3/3 fingerprint matches
9. 5 real AI queries: 5/5 HTTP 201
10. SQLite restored to post-migration baseline
11. Lockfiles unchanged

## v1.0.13 Verdict

FAIL - due to:
- Playwright focal not passing
- Playwright full not passing
- Evidence incomplete

## v1.0.13 Files Moved

Moved to `rejected/` folder:
- `APPCAJA-V3-AI-FINGERPRINT-CONSISTENCY-AND-FINAL-VALIDATION-evidence-v1.0.13/`

## Acceptance of v1.0.13 Fix

The fingerprint fix implemented in v1.0.13 was accepted as correct and frozen. The issue was not with the fix but with:
1. Test structure (single test with 2 AI queries exceeding 240s)
2. Evidence completeness
3. Lint errors from playwright-report artifacts
