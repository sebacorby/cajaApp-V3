# APP-UX-PRIVACY-002 v1.0.2

Estado: FAIL focal.

El mensaje de guardado quedó corregido. La ejecución se detuvo por un selector Playwright ambiguo que encontraba las vistas `movement-row` y `movement-card` del mismo movimiento.

No se cambió código productivo. Se ajustaron únicamente las dos aserciones del test para usar el `movementId` y el contenedor visible.

Continúa la revalidación v1.0.3.