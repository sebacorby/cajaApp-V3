# Playwright discovery

## Estado

El gate de Playwright no pudo ejecutarse debido a que el ecosistema no pudo arrancar mediante el script headless autoritativo.

Se seleccionaron puertos libres para el arranque:
- Backend: 11436
- Frontend: 11437

El arranque headless fall en `prisma:generate` por BOM en `schema.prisma`.

Adems, el frontend no pudo compilar (`typecheck` EXIT=2) por referencias rotas a mdulos duplicados:
- `src/components/finance/layout/header.tsx` importa `@/components/finance/search/global-search-dialog` (existe el archivo duplicado `global-search-dialog (1).tsx`, no el cannico).
- `src/components/finance/sections/section-router.tsx` importa `@/components/finance/search/search-target-banner` (existe el archivo duplicado `search-target-banner (1).tsx`, no el cannico).

## Discovery no realizado

No se ejecut `npm exec -- playwright test --list` ni ningn otro mecanismo de discovery porque no existe un punto de entrada estable para la suite de tests.

Especs relevantes del frontend (sin ejecutar):
- `tests/categories.spec.ts` (cannico)
- `tests/categories (1).spec.ts` (duplicado, no debe ejecutarse)
- `tests/ai-advisor.spec.ts`
- `tests/financial-health.spec.ts`
- `tests/dashboard-trend-visual.spec.ts`
- `tests/quality-audit.spec.ts`

Resultado: NOT RUN.
