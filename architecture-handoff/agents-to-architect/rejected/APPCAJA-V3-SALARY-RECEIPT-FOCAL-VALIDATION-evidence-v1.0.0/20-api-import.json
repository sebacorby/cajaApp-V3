# API Smoke Test — v1.0.0 Salary Receipt Focal Validation

## Date: 2026-07-16

## Status: BLOCKED

## Reason

The backend build FAILS with TypeScript errors:
1. `salary-receipt-extraction.service.ts(256,7)` - `warnings` type incompatibility
2. `salary-receipts.controller.ts(53,32)` - Zod schema type incompatibility

Since the build fails, we cannot compile the backend and therefore cannot start it for API testing.

## Required Routes (NOT TESTED)

- POST /api/salary-receipts/import
- GET /api/salary-receipts/drafts/:draftId
- PUT /api/salary-receipts/drafts/:draftId
- POST /api/salary-receipts/drafts/:draftId/accept
- GET /api/salary-receipts
- POST /api/salary-receipts/:receiptId/reverse

## Scenario

Cannot execute the mandatory API scenario:
1. Import PDF sanitized
2. Receive HTTP successful
3. Get draftId
4. Check draft status
5. Check document, AI run, and preview
6. Edit at least one concept
7. Send updated draft
8. Verify backend recalculates totals
9. Accept draft
10. Verify receipt creation
11. Verify real income creation
12. Verify history
13. Reverse receipt
14. Verify associated events are removed

## Conclusion

❌ FAIL - Cannot run API smoke tests due to build failure.
